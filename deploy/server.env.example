PORT=8080
HOST=your-domain-or-ip
BIND_HOST=0.0.0.0
DEFAULT_ROOM=lobby
HEARTBEAT_INTERVAL=30000
MAX_PAYLOAD_BYTES=16384
# Comma-separated browser origins. Empty permits native clients and all origins.
ALLOWED_ORIGINS=
